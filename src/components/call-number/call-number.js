import React from 'react';
import PropTypes from 'prop-types';
import styles from './call-number.module.css';

const CallNumber = () => (
  <div className={styles.CallNumber}>
    CallNumber Component
  </div>
);

CallNumber.propTypes = {};

CallNumber.defaultProps = {};

export default CallNumber;
